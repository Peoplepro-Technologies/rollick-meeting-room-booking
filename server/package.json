{
  "name": "meeting-room-booking-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/app.js",
    "start": "node src/app.js",
    "db:generate": "prisma generate",
    "db:generate:prod": "cp prisma/schema.postgresql.prisma prisma/schema.prisma && prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:migrate:prod": "cp prisma/schema.postgresql.prisma prisma/schema.prisma && prisma migrate dev",
    "db:studio": "prisma studio",
    "db:seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "validator": "^13.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.6.0"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  }
}